# The repo's gate, run where nobody's machine is involved. Same commands as
# CLAUDE.md's Conventions section: lint + test + build in web/, pytest in
# sidecar/. Linux and UTC, where development happens on Windows in a local
# timezone, so a test that only passes because of either shows up here.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web:
    name: web — lint, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm test
      # tsc -b then vite build. `tsc --noEmit` would check nothing here: the
      # root tsconfig.json is a solution file with "files": [].
      - run: npm run build

  sidecar:
    name: sidecar — pytest
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: sidecar
    steps:
      - uses: actions/checkout@v7
      # Two tests in tests/test_allowlist.py assert that the app mounts the
      # built frontend, and create_app() only mounts it when web/dist exists.
      # They pass on a dev machine because someone ran `npm run build` earlier,
      # and fail on a fresh checkout. Build it here until those tests supply
      # their own web_dist. Then these three steps can go.
      - uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci && npm run build
        working-directory: web
      - uses: astral-sh/setup-uv@v10
        with:
          # pyproject allows >=3.11,<3.13; 3.12 is what development runs on.
          python-version: '3.12'
          enable-cache: true
          cache-dependency-glob: sidecar/uv.lock
      # --locked fails if uv.lock is out of step with pyproject.toml, rather
      # than quietly re-resolving to versions nobody has tested.
      - run: uv sync --locked
      - run: uv run pytest
