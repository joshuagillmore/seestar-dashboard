# Node / frontend
node_modules/
dist/
build/
.next/
*.tsbuildinfo

# Python (if a thin backend appears)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Env & secrets — never commit real values
.env
.env.local
*.env.local
secrets/
*.pem
*.key

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code personal layer (per-machine, not shared)
CLAUDE.local.md
.claude/settings.local.json

# SDD / agent working artifacts — scratch, never committed
.superpowers/

# OpenNGC raw source CSVs — ~3.7 MB, not ours to redistribute as raw source.
# Regenerate via data/build_catalogue.py; see data/README.md for the download
# URLs and the SHA-256 of the snapshot the committed catalogue was built from.
data/openngc/

# Fetched DSS2 cutouts — see sidecar/seestar_sidecar/imagery.py. Re-fetchable
# from CDS hips2fits at any time; never the only copy of anything.
sidecar/.cache/

# Recorded fixtures may capture site coordinates. Committed ones are rounded to
# ~11 km deliberately (see fixtures/README.md); a full-precision re-record must
# not be committed.
